# Artifacts

Training outputs are written here.

- `models/` stores the serialized training pipeline and schema metadata.
- `metrics/` stores evaluation outputs generated during training.

Large binary artifacts should stay out of git.
